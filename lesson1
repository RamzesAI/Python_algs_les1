
'''
1. Найти сумму и произведение цифр трехзначного числа, которое вводит пользователь.
'''

a = input('Введите трехзначное число: ')
sum_of_numbers = 0
multiply_of_numbers = 1
for i in a:
    sum_of_numbers += int(i)
    multiply_of_numbers *= int(i)
print(f'Сумма цифр равна: {sum_of_numbers}')
print(f'Произведение цифр равно: {multiply_of_numbers}')

'''
Задание 2
1.Выполнить логические побитовые операции «И», «ИЛИ» и др. над числами 5 и 6.
2.Выполнить над числом 5 побитовый сдвиг вправо и влево на два знака. Объяснить полученный результат.
'''
'''
1 часть.
Операция «И» при вычислении каждого бита 5 (101) и 6 (110) даст 4 (100)
Операция «ИЛИ» при вычислении каждого бита 5 (101) и 6 (110) даст 7 (111)
Операция «ИСКЛЮЧАЮЩЕЕ ИЛИ» при вычислении каждого бита 5 (101) и 6 (110) даст 3 (011)

'''
print(5 & 6, ':', bin(5 & 6))
print(5 | 6, ':', bin(5 | 6))
print(5 ^ 6, ':', bin(5 ^ 6))


'''
2 часть.
Выполнение побитового сдвига влево на 1 знак обозначает умножение на 2 (добавляем 1 ноль в двоичном коде)
Выполнение побитового сдвига вправо на 1 знак обозначает целочисленное деление на 2 с отбрасыванием 
чисел после запятой (убираем 2 знака справа в двоичном коде)

Для 5 побитовый сдвиг влево на 2 знака даст 20, для 6 даст 24
Для 5 побитовый сдвиг вправо на 2 знака даст 1, для 6 даст также 1
'''
print(bin(5 << 2)) # аналагично переводу 20 в двоичную систему
print(bin(6 << 2)) # аналагично переводу 24 в двоичную систему
print(bin(5 >> 2)) # аналагично переводу 1 в двоичную систему
print(bin(6 >> 2)) # аналагично переводу 1 в двоичную систему

'''
3. По введенным пользователем координатам двух точек вывести уравнение прямой вида y=kx+b, проходящей через эти точки.
'''

'''
Коэффициенты k и b вычисляются по координатам, введенным пользователем:
y1 = kx1 + b
y2 = kx2 + b
Получаем, что:
b = y2 - kx2
k = (y1-y2) / (x1 - x2)
'''
print('Задаем координаты x1, y1 первой точки и x2, y2 второй точки')
x1 = int(input('Введите координату х1 первой точки: '))
y1 = int(input('Введите координату y1 первой точки: '))
x2 = int(input('Введите координату х2 второй точки: '))
y2 = int(input('Введите координату y2 второй точки: '))

k = (y1 - y2) / (x1 - x2)
b = y2 - k * x2

if b < 0:
    print('Уравнение прямой принимает вид: y=%.2fx%.2fb' % (k, b))
else:
    print('Уравнение прямой принимает вид: y=%.2fx+%.2fb' % (k, b))

    '''
4. Написать программу, которая генерирует в указанных пользователем границах:
случайное целое число;
случайное вещественное число;
случайный символ.
Для каждого из трех случаев пользователь задает свои границы диапазона. Например,
если надо получить случайный символ от 'a' до 'f', то вводятся эти символы.
Программа должна вывести на экран любой символ алфавита от 'a' до 'f' включительно.
'''
## Для целых чисел
ls = [1, 2, 0, 3, 5, 7, 4]
for i in ls:
    left_border = int(input('Укажите левую границу диапазона: '))
    right_border = int(input('Укажите правую границу диапазона: '))
    res = (right_border + left_border + i) // 2
    if res > right_border:
        res = right_border
    if res < left_border:
        res = left_border
    print(res)

## Для вещественных чисел
ls = [1.29, 2.14, 0.78, 3.19, 5.27, 7.45, 4.11]
for i in ls:
    left_border = int(input('Укажите левую границу диапазона: '))
    right_border = int(input('Укажите правую границу диапазона: '))
    res = ((right_border + left_border + i ) / 2)
    if res > right_border:
        res = right_border - 0.17
    if res < left_border:
        res = left_border + 0.49
    print(res)

## Для символов
import string
letters_list = string.ascii_lowercase
ls = [1, 2, 0, 3, 5, 7, 4]
for i in ls:
    user_input = input('Введите подряд две буквы латинского алфавита: ').lower()
    user_input.split()
    left_border = letters_list.index(user_input[0])
    right_border = letters_list.index(user_input[1])
    res = (right_border + left_border + i) // 2
    if res > right_border:
        res = right_border
    if res < left_border:
        res = left_border
    print(letters_list[res])


'''
5. Пользователь вводит две буквы. Определить, на каких местах алфавита они стоят и сколько между ними находится букв.
'''

import string
letters_list = string.ascii_lowercase
user_input = input('Введите подряд две буквы латинского алфавита: ').lower()
user_input.split()
print(f'Буква "{user_input[0]}" в алфавите под номером {letters_list.index(user_input[0]) + 1}')
print(f'Буква "{user_input[1]}" в алфавите под номером {letters_list.index(user_input[1]) + 1}')
numbers_of_letters = letters_list.index(user_input[1]) - letters_list.index(user_input[0])
print(f'Количество букв между "{user_input[0]}" и "{user_input[1]}": {abs(numbers_of_letters) -1}')

'''
6. Пользователь вводит номер буквы в алфавите. Определить, какая это буква.
'''

import string
letters_list = string.ascii_lowercase
user_input = int(input('Введите номер буквы латинского алфавита (их всего 26): ').lower()) - 1
print(f'Номеру {user_input + 1} соответствует буква "{letters_list[user_input]}" латинского алфавита')


'''
7. По длинам трех отрезков, введенных пользователем, определить возможность существования треугольника,
составленного из этих отрезков.
Если такой треугольник существует, то определить, является ли он разносторонним, равнобедренным или равносторонним.
'''

## Треугольник существует, если две любые его стороны больше третьей.
print('Необходимо ввести стороны треугольника: a, b, c')
a = int(input('Укажите длину стороны "a": '))
b = int(input('Укажите длину стороны "b": '))
c = int(input('Укажите длину стороны "c": '))

if a + b > c and b + c > a and a + c > b:
    if a != b and a != c and b != c:
        print('Треугольник разносторонний')
    if a == b == c:
        print('Треугольник равносторонний')
    elif a == b or b == c or a == c:
        print('Треугольник равнобедренный')
else:
    print('Такой треугольник невозможно построить')


'''
8. Определить, является ли год, который ввел пользователь, високосный или невисокосный.
'''

year_date = int(input('Введите любой год: '))
if year_date % 4 == 0:
    print(f'{year_date} високосный год')
else:
    print(f'{year_date} невисокосный год')


 '''
9. Вводятся три разных числа. Найти, какое из них является средним (больше одного, но меньше другого).
'''
list_numbers = []
first_number = int(input('первое число: '))
list_numbers.append(first_number)
second_number = int(input('второе число: '))
list_numbers.append(second_number)
third_number = int(input('третье число: '))
list_numbers.append(third_number)
list_numbers.sort()
print(f'{list_numbers[1]} является средним числом')
   
